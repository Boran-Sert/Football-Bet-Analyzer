# ═══════════════════════════════════════════════
#  Football SaaS — Production Dependencies
#  S-18 Fix: Tum paketler pinlendi (reproducible builds)
# ═══════════════════════════════════════════════

# ── Core Framework ──
fastapi>=0.136.0,<1.0.0
uvicorn[standard]>=0.46.0,<1.0.0
gunicorn>=22.0.0,<23.0.0

# ── Database & Cache ──
motor>=3.7.0,<4.0.0
redis>=7.4.0,<8.0.0

# ── Data Validation ──
pydantic>=2.13.0,<3.0.0
pydantic-settings>=2.14.0,<3.0.0

# ── HTTP Client ──
httpx>=0.28.0,<1.0.0

# ── Auth & Security ──
passlib>=1.7.4,<2.0.0
bcrypt==3.1.7
PyJWT>=2.12.0,<3.0.0
python-multipart>=0.0.27,<1.0.0

# ── Serialization ──
orjson>=3.11.0,<4.0.0

# ── Async Utilities ──
aiofiles>=25.0.0,<26.0.0
apscheduler>=3.11.0,<4.0.0

# ── Payment Providers ──
stripe>=15.0.0,<16.0.0
iyzipay>=1.0.46,<2.0.0

# ── Email ──
resend>=2.0.0,<3.0.0

# ── Observability ──
prometheus-client>=0.21.0,<1.0.0
prometheus-fastapi-instrumentator>=7.0.0,<8.0.0

# ── Test (dev only) ──
pytest>=9.0.0,<10.0.0
pytest-asyncio>=1.3.0,<2.0.0
pytest-mock>=3.15.0,<4.0.0